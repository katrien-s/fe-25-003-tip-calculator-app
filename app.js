const amountOfBillEl = document.getElementById('bill');
const numberOfPeopleEl = document.getElementById('number-of-people');
// const submitBtn = document.getElementById('submit');

const amountOfBill = amountOfBillEl.value;
const numberOfPeople = numberOfPeopleEl.value;

// console.log(submitBtn);

// How to get the value read & automatically calculated without there being a button to submit the values
// Do I need to simulate the button? How does JS/browser read the values without submitting them

// Reset is easy
